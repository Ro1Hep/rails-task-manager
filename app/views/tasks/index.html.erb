<h1>All Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <input type=checkbox <%= task[:completed] == true ? "checked" : ""%>>
      <%= link_to task[:title], details_path(task)%>
    </li>
  <% end %>
</ul>
